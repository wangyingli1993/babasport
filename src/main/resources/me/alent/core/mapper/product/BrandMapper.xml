<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.alent.core.mapper.product.BrandMapper">

    <resultMap id="brand" type="Brand">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="img_url" property="imgUrl"/>
        <result column="sort" property="sort"/>
        <result column="is_display" property="isDisplay"/>
    </resultMap>

    <sql id="get_brand_where">
        <if test="isDisplay != null">
            is_display = #{isDisplay}
        </if>
        <if test="name != null and name != ''">
            and name LIKE CONCAT('%', #{name}, '%')
        </if>
    </sql>

    <update id="updateBrandByKey" parameterType="Brand">
        UPDATE bbs_brand
        <set>
            <if test="name != null">
                name=#{name},
            </if>
            <if test="description != null">
                description=#{description},
            </if>
            <if test="imgUrl != null">
                img_url=#{imgUrl},
            </if>
            <if test="sort != null">
                sort=#{sort},
            </if>
            <if test="isDisplay != null">
                is_display=#{isDisplay}
            </if>
        </set>
        <where>
            id=#{id}
        </where>
    </update>

    <!--查询品牌-->
    <select id="getBrandListWithPage" parameterType="BrandPagination" resultMap="brand">
		SELECT id, name, description, img_url, sort, is_display
        FROM bbs_brand
        <where>
            <include refid="get_brand_where"/>
        </where>
        ORDER BY id DESC
        LIMIT #{startRow}, #{pageSize}
    </select>
    <!--sql中不要使用count(*), 太浪费性能，使用count(1)-->
    <select id="getBrandCount" parameterType="Brand" resultType="int">
        SELECT count(1)
        FROM bbs_brand
        <where>
            <include refid="get_brand_where"/>
        </where>
    </select>

    <select id="getBrandByKey" resultMap="brand">
        SELECT id, name, description, img_url, sort, is_display
        FROM bbs_brand
        <where>
            id = #{id}
        </where>
    </select>

    <!--添加品牌-->
    <insert id="addBrand" parameterType="Brand">
        INSERT INTO bbs_brand
        <trim prefix="(" suffix=")">
            name,
            description,
            img_url,
            sort,
            is_display
        </trim>
        VALUE
        <trim prefix="(" suffix=")" >
            #{name},
            #{description},
            #{imgUrl},
            #{sort},
            #{isDisplay}
        </trim>
    </insert>

    <!--删除-->
    <delete id="deleteBrandByKey" parameterType="int">
        DELETE FROM bbs_brand
        <where>
            id = #{id}
        </where>

    </delete>

    <!--批量删除-->
    <delete id="deleteBrandByKeys" parameterType="int">
        DELETE FROM bbs_brand
        <where>
            id in
            <foreach collection="array" item="id" open="(" close=")" separator=",">
                #{id}
            </foreach>
        </where>
    </delete>
</mapper>
